h = float(input("Digite la altura en donde se deja caer el objeto (metros):"))
v = float(input("Digite la velocidad cuando el objeto tocó el suelo (seg):"))
print("El tiempo en segundos que demoro el objeto despues de haber caido de determinada altura de", h, "metros es de :", (2 * h) / v)
